<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <ul>
            <li>
                <a href="{{ path('home') }}">home</a>
            </li>
            <li>
                <a href="{{ path('contact') }}"> ajouter un contacte</a>
            </li>
            <li>
                <a href="{{ path('label') }}"> ajouter un label</a>
            </li>
             <li>
                <a href="{{ path('email') }}"> ajouter un email</a>
            </li>
        </ul>
        {% block body %}{% endblock %}
    </body>
</html>
